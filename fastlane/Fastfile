default_platform(:ios)

platform :ios do
  desc "Build IPA for CI"
  lane :build_local do
    build_app(
      scheme: "CICD_Pipelines",
      skip_codesigning: true,
      output_directory: "./build",
      output_name: "CICD_Pipelines.ipa"
    )
  end
end